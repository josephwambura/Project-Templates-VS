<ContentPage xmlns:pageModels="clr-namespace:MauiSampleApp.PageModels"
             xmlns:models="clr-namespace:MauiSampleApp.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.TextInputLayout;assembly=Syncfusion.Maui.Toolkit"
             x:DataType="pageModels:SignUpPageModel"
             x:Class="MauiSampleApp.Pages.SignUpPage"
             x:Name="SignUp"
             Title="Sign Up">
    <ContentPage.Resources>
        <Style x:Key="InvalidEntryStyle" TargetType="Entry">
            <Setter Property="TextColor" Value="Red" />
        </Style>

        <Style TargetType="Entry">
            <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}" />
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="FontFamily" Value="OpenSansRegular"/>
            <Setter Property="FontSize" Value="{OnIdiom 16,Desktop=24}" />
            <Setter Property="PlaceholderColor" Value="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray500}}" />
            <Setter Property="MinimumHeightRequest" Value="44"/>
            <Setter Property="MinimumWidthRequest" Value="44"/>
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray600}}" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Spacing="{StaticResource LayoutSpacing}" Padding="{StaticResource LayoutPadding}" VerticalOptions="Center">

            <sf:SfTextInputLayout Hint="Username">
                <Entry Text="{Binding UserName}" SemanticProperties.Description="{Binding UserName}" />
            </sf:SfTextInputLayout>

            <sf:SfTextInputLayout Hint="Confirm Username">
                <Entry Text="{Binding ConfirmUserName}" SemanticProperties.Description="{Binding ConfirmUserName}" />
            </sf:SfTextInputLayout>

            <sf:SfTextInputLayout Hint="Password" EnablePasswordVisibilityToggle="True">
                <Entry Text="{Binding Password}" SemanticProperties.Description="{Binding Password}" IsPassword="True" />
            </sf:SfTextInputLayout>

            <sf:SfTextInputLayout Hint="Confirm Password" EnablePasswordVisibilityToggle="True">
                <Entry Text="{Binding ConfirmPassword}" SemanticProperties.Description="{Binding ConfirmPassword}" IsPassword="True" />
            </sf:SfTextInputLayout>

            <Button Text="Sign Up" 
                    Command="{Binding SignUpCommand}"
                    SemanticProperties.Description="Sign Up"
                    MinimumHeightRequest="{OnIdiom 44, Desktop=60}"
                    HorizontalOptions="Fill" />

            <HorizontalStackLayout HorizontalOptions="Center" Spacing="5">
                <Label Text="Already have an account?" 
                        VerticalOptions="Center" 
                        TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}" />

                <Button Text="Sign In"
                        Command="{Binding SignInCommand}"
                        SemanticProperties.Description="Navigate to Sign In"
                        BackgroundColor="Transparent"
                        TextColor="{StaticResource Primary}"
                        FontAttributes="Bold"
                        Padding="0"
                        HeightRequest="44"
                        VerticalOptions="Center" />
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>